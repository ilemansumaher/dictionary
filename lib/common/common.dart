import 'package:flutter/material.dart';

const materialColor = Color(0xFF0D1E25);
const borderColor = Color(0xFFF1F1F1);
const textColorInSearch = Color(0xFFB1B1B1);
const secondaryColor = Color(0xFF4B33E1);

const materialColorDark = Color(0xFF000000);       // Very dark background
const borderColorDark = Color(0xFFe6e6e6);         // Subtle border, low contrast
const textColorInSearchDark = Color(0xFFAAAAAA);   // Muted light gray for placeholder text
const secondaryColorDark = Color(0xFF8A7FFF);      // Lighter version of your primary purple
const backgroundColorDark = Color(0xFF787878);      // Lighter version of your primary purple
